submarine:
  # Address of the AMQP server to connect to.
  amqp_server_address: "amqp://127.0.0.1:5672/%2f"
  # Address to expose the HTTP API on.
  http_server_listen_address: "0.0.0.0:3069"
  # Address to expose Prometheus on.
  prometheus_listen_address: "127.0.0.1:6969"
  # A list of device config files to load.
  device_configs: [
  ]
